import { DropCardDirective } from './drop-card.directive';

describe('DropCardDirective', () => {
  it('should create an instance', () => {
    const directive = new DropCardDirective();
    expect(directive).toBeTruthy();
  });
});
